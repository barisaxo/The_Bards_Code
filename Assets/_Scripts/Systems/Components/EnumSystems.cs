using System;

public static class EnumSystems
{
    //public static int Length<TEnum>(this TEnum e) =>
    //    Enum.GetNames(e.GetType()).Length;


    //public static TEnum Last<TEnum>(this TEnum e) =>
    //    (TEnum)Enum.GetValues(e.GetType()).GetValue(e.Length() - 1);
}
